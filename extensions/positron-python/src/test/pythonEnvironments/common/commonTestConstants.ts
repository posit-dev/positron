import * as path from 'path';

export const TEST_LAYOUT_ROOT = path.join(
    __dirname,
    '..',
    '..',
    '..',
    '..',
    'src',
    'test',
    'pythonEnvironments',
    'common',
    'envlayouts',
);
