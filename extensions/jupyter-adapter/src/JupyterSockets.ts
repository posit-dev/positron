/*---------------------------------------------------------------------------------------------
 *  Copyright (c) RStudio, PBC.
 *--------------------------------------------------------------------------------------------*/

export enum JupyterSockets {
    shell = 'shell',
    iopub = 'iopub',
    heartbeat = 'heartbeat',
    stdin = 'stdin',
    control = 'control'
}
