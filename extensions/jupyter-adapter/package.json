{
  "name": "jupyter-adapter",
  "displayName": "Positron Jupyter Adapter",
  "description": "Jupyter Kernel Protocol adapter for Positron Language Runtime",
  "publisher": "Posit Software, PBC",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.61.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Jupyter Heartbeat Interval",
      "properties": {
        "positron.jupyter.heartbeat": {
          "scope": "window",
          "type": "number",
          "default": 10,
          "description": "The interval between kernel heartbeat requests (0 to disable heartbeat checks)"
        }
      }
    }
  },
  "scripts": {
    "compile": "npm run compile",
    "watch": "npm run watch"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.3",
    "@types/vscode": "^1.61.0",
    "@types/zeromq": "^5.2.1"
  },
  "dependencies": {
    "uuid": "^8.3.2",
    "zeromq": "^6.0.0-beta.6",
    "vscode-nls": "^5.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rstudio/myriac"
  }
}
